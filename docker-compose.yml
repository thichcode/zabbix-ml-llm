version: "3.9"

services:
  sre-copilot:
    build: .
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./app/mocks:/app/app/mocks:ro
    restart: unless-stopped
